# Legacy GEO guide URLs now live under /ai-search
/guides /ai-search 301
/guides/ /ai-search 301
/guides/:slug /ai-search/:slug 301
/guides/:slug/ /ai-search/:slug 301

# Serve pre-rendered static AI-search pages directly
/ai-search/index.html /ai-search/index.html 200
/ai-search/:slug /ai-search/:slug/index.html 200

# Serve static files (sitemap, robots, llms.txt, etc.) directly
/sitemap.xml /sitemap.xml 200
/sitemap-answers.xml /sitemap-answers.xml 200
/robots.txt /robots.txt 200
/llms.txt /llms.txt 200
/.well-known/ai-answers.json /.well-known/ai-answers.json 200
/.well-known/llms.txt /.well-known/llms.txt 200

# SPA fallback for everything else
/* /index.html 200
